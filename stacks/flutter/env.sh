# Flutter stack runtime env — sourced by rebuild.sh (host-side) to compute
# the container's env_file. Not sourced inside the container itself.
export FLUTTER_HOME=/home/node/flutter
export PATH="$FLUTTER_HOME/bin:$PATH"
