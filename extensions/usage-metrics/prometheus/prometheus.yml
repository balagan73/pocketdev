global:
  scrape_interval: 30s

scrape_configs:
  - job_name: openclaw
    scrape_interval: 30s
    metrics_path: /api/diagnostics/prometheus
    authorization:
      credentials_file: /etc/prometheus/openclaw-token
    static_configs:
      - targets: ["openclaw:18789"]
