# pocket-dev configuration
# Edit this file and run ./rebuild.sh to apply changes,
# or run ./setup.sh to reconfigure interactively.

stacks:
  - android-sdk
  - chrome
  - flutter
  - linux-desktop

extensions:
  - discord-voice
  - piper
  - telegram
  - usage-metrics
  - whisper
